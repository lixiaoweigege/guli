#服务端口
server.port=8100

# 服务名
spring.application.name=guli-eureka

# 环境设置：dev、test、prod
spring.profiles.active=dev

#Eureka客户端与Eureka服务端进行通信的地址,通常情况下不需要注册，只有在配置注册中心集群时候使用
#eureka.client.service-url.defaultZone=http://localhost:8100/eureka